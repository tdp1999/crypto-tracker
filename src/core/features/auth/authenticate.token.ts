// Regardless of auth token
export const METADATA_PUBLIC = Symbol('METADATA_PUBLIC');

// Suppose to reject request if it has auth token
export const METADATA_REQUIRE_NO_AUTH = Symbol('METADATA_REQUIRE_NO_AUTH');
